/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    images: {
      domains: ['inlighte.com'], // Add your image domain here
    },
  };

export default nextConfig;
